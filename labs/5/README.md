* To train and use logistic regression, run 
  ```
  make LogisticRegression
  ```
* To train and use perceptron, run 
  ```
  make perceptron
  ```
* To clean logs and output files, run 
  ```
  make clean
  ```
